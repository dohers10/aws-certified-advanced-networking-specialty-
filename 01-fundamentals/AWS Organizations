# AWS ORG
- only 1 mgmt/master account (creates Org) - lots of member accounts
- Organizational Root ( container) contains OUs ( dev, prod, etc)
- Allows consolidated billing. Payer account = master/mgmt account.
- Allows for SCPs to target OUs
- Can create accounts within the ORG

